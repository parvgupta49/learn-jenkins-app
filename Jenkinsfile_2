pipeline {
    agent {
        docker {
            image 'node:18-alpine'
        }
    }
    stages {
        stage('Hello') {
            steps {
                sh 'npm ci'
                sh 'npm run build'
            }
        }
    }
}
